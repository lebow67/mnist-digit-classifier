# mnist-digit-classifier
A neural network (built with TensorFlow/Keras) that classifies hand-drawn digits (0-9). Trained on the full MNIST dataset (60,000 images), achieving ~97.6% test accuracy. Deployed with Gradio's Sketchpad for live drawing-based predictions.
